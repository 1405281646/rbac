# RBAC0 权限管理系统 — 领域上下文

## 领域术语

| 术语 | 定义 |
|------|------|
| **User** | 系统用户，必选属于一个部门，通过角色关联获取权限 |
| **Role** | 角色，支持无限级树形 | 权限不删除，和自定义角色、菜单 + 按钮/接口 级别 |**系统内置角色**不可删除 |
| **Permission** | 权限，粒度分为「菜单权限」和「按钮/接口权限」，通过角色分配给用户 |
| **Company** | 公司实体，支持层级树形结构（集团 → 子公司 → 分公司等） |
| **Dept** | 部门，必选属于某个公司，支持无限级树形层级（总公司 → 区域 → 分公司 → 部门） |
| **User–Dept** | N:1 — 一个用户必须属于一个部门 |
| **User–Role** | N:M — 通过用户-角色关联表实现 |
| **Role–Permission** | N:M — 通过角色-权限关联表实现 |
| **Company–Dept** | 1:N — 一个公司有多个部门 |
| **Dept–Parent** | 自引用 N:1 — 部门可设置上级部门 |

## 技术栈

- **后端**: Spring Boot 3.x + JDK 17 + MyBatis-Plus 3.5.x + MySQL 8.x
- **前端**: Vue 3 + Vite + Element Plus
- **认证**: JWT (Access Token) + Redis 黑名单
- **验证码**: 可配置开关（配置项控制）